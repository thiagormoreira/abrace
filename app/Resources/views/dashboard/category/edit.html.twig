{% extends 'dashboard.html.twig' %}

{% block body %}
    <div class="row bg-title">
        <div class="col-lg-3 col-md-4 col-sm-4 col-xs-12">
            <h4 class="page-title">Categoria <small>Editar categoria</small></h4>
        </div>
    </div>
    {{ form_start(edit_form, {
        'attr': {
            'class': 'form-horizontal'
        }
    }) }}
    <!-- .row -->
    <div class="row">
        <div class="col-sm-12">
            <div class="white-box">
                <div class="form-group">
                    <div class="form-group">
                        {{ form_label(edit_form.name, null, {
                            'label': 'Categoria: ',
                            'label_attr': {
                                'class': 'control-label'
                            }
                        }) }}
                        {{ form_widget(edit_form.name, {
                            'attr': {
                                'class': 'form-control'
                            }
                        }) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(edit_form.description, null, {
                            'label': 'Status do artigo: ',
                            'label_attr': {
                                'class': 'control-label'
                            }
                        }) }}
                        {{ form_widget(edit_form.description, {
                            'attr': {
                                'class': 'form-control',
                            }
                        }) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- /.row -->
    <!-- .row -->
    <div class="row">
        <div class="col-sm-12">
            <div class="white-box">
                <div class="form-group">
                    <ul class="list-inline pull-right">
                        <li>
                            <a href="{{ path('category_index') }}">Voltar</a>
                        </li>
                        <li>
                            <input type="submit" class="btn btn-primary"
                                   value="Salvar" />
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
    {{ form_end(edit_form) }}
{% endblock %}

{% block stylesheets %}
{% endblock %}

{% block javascripts %}
{% endblock %}